{
  "name": "@kiskadee/web-builder",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "run-build.js",
  "exports": {
    ".": "./run-build.js",
    "./types": "./types.ts"
  },
  "scripts": {
    "build": "node --experimental-strip-types --no-warnings ./src/run-build.ts",
    "sync": "node --experimental-strip-types --no-warnings ./scripts/sync-showcase-artifacts.ts",
    "generate": "node --experimental-strip-types --no-warnings ./scripts/generate-showcase-registry.ts",
    "build-sync-generate": "pnpm run build && pnpm run sync && pnpm run generate",
    "vitest-ui": "vitest --ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@kiskadee/core": "workspace:^",
    "@kiskadee/css-build": "workspace:^",
    "@kiskadee/presets": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "esbuild": "^0.25.9",
    "postcss": "^8.5.6",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
