# Technical Debt

This directory tracks deferred cleanup and follow-up work that is concrete enough to preserve, but
not urgent enough to do immediately.

Use one file per debt item for debt owned by `@kiskadee/web-builder`.

Debt owned by another package should live under that package's own `docs/` directory.

## Current Items

- [Future optimizations](future-optimizations.md) - deferred web-builder pipeline optimization debt.
