import type { IconProps } from '../../Icon.types.ts';
import { VolumeIconBase } from '../../internal/kiskadee/VolumeIconBase.tsx';

export function VolumeIcon(props: IconProps) {
  return <VolumeIconBase {...props} />;
}
